@using EntityLayer.Concrete
@model List<Announcement>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_WriterLayout.cshtml";
}
@{
    string color = "";
}
<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Basic Table</h4>
            <div class="table-responsive">
                <table class="table">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Title</th>
                            <th>Date</th>
                            <th>Status</th>
                            <th>Details</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            if (item.Status == "New Content")
                            {
                                color = "badge badge-danger";
                            }
                            else if (item.Status == "New Video")
                            {
                                color = "badge badge-warning";
                            }
                            else if (item.Status == "Workshop")
                            {
                                color = "badge badge-info";
                            }
                            else if (item.Status == "New Speech")
                            {
                                color = "badge badge-success";
                            }
                            else if (item.Status == "Bootcamp")
                            {
                                color = "badge badge-primary";
                            }

                            <tr>
                                <td>@item.ID</td>
                                <td>@item.Title</td>
                                <td>@(((DateTime)item.Date).ToString("dd-MMM-yyyy"))</td>
                                <td><label class="@color">@item.Status</label></td>
                                <td><a href="/Users/Default/Details/@item.ID" class="badge badge-primary">Details</a></td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
